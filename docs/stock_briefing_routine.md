# 📊 주식 시황 브리핑 루틴 (v4)

> **실행 조건**: 스케줄러에 의해 자동 실행 (KST 23:25 이후)
> **실행 환경**: Claude Code 클라우드 루틴
>   - repo가 clone된 컨테이너 내부에서 실행
>   - 파일 저장·git 명령은 모두 상대경로 사용
>   - GitHub push는 루틴에 연결된 repo 권한으로 처리 (별도 토큰 불필요)
>   - 수집 종목 관리: `./config/watchlist.yaml` (루틴 프롬프트 수정 불필요)

---

## [Step 1: 데이터 수집]

### 1-1. 종목 목록 로드

Read tool로 `./config/watchlist.yaml`을 읽어 수집할 티커 목록을 확인하라.
파일 구조: `indices` / `macro` / `us_indices` / `sectors` 4개 섹션으로 구성.
이후 모든 수집은 이 파일에 정의된 티커를 기준으로 한다.

### 1-2. PlayMCP UsStockInfo — 주가/지표 수집

watchlist.yaml에 정의된 **모든 티커**를 순서대로 `get_stock_info`로 수집하라.
- 각 티커는 **개별 호출**한다
- 호출 실패·응답 없음·null 값인 경우 해당 항목만 `"N/A"`로 기록하고
  **다음 티커로 반드시 계속 진행**한다
- 하나의 실패가 전체 루틴을 중단시켜서는 안 된다

### 1-3. PlayMCP NaverSearch — 뉴스 수집

아래 4개 쿼리로 뉴스를 수집하라.
각 쿼리 실패 시 해당 결과만 빈 배열로 처리하고 진행 계속.

| 쿼리 | 결과 수 | 목적 |
|------|---------|------|
| `"코스피 오늘 종가 시황"` | 5건 | 당일 지수 흐름 |
| `"외국인 기관 순매수 오늘"` | 3건 | 수급 동향 |
| `"국내 증시 테마주 오늘"` | 3건 | 섹터/테마 이슈 |
| `"미국 증시 영향 국내"` | 3건 | 글로벌 연계 이슈 |

---

## [Step 2: 리포트 작성]

### 2-1. 템플릿 처리

Read tool로 `templates/stock_report.md`를 읽어라.
- **읽기 성공**: 해당 파일의 섹션 구조·헤더·마커를 그대로 따른다
- **읽기 실패**: 아래 2-2 내장 구조로 대체하고,
  리포트 최상단에 `<!-- 템플릿 로드 실패: 내장 구조 사용 -->` 기록

### 2-2. 리포트 내장 구조 (fallback)

`{{ 변수 }}` 표기는 Jinja2 엔진이 아니라 **수집한 실제 데이터로 직접 텍스트 치환**하라.
섹터 테이블은 watchlist.yaml의 `sectors` 항목 수에 맞게 자동으로 행을 추가한다.

```
# 📊 주식 시황 브리핑 — {{ YYYY-MM-DD }} (장 마감)

## 🌡️ 시장 온도계: {{ 🔴리스크오프 | 🟡중립 | 🟢리스크온 }}

> **판단 근거**: {{ 온도 판단의 핵심 이유 1~2문장 }}

---

## ① 국내 지수

| 지수 | 종가 | 등락 | 등락률 |
|------|------|------|--------|
| 코스피 | {{ val }} | {{ ±pts }} | {{ ±% }} |
| 코스닥 | {{ val }} | {{ ±pts }} | {{ ±% }} |

---

## ② 매크로 지표

| 지표 | 현재값 | 전일대비 |
|------|--------|---------|
| 원/달러 환율 | {{ val }}원 | {{ ±val }} |
| 미 10년물 금리 | {{ val }}% | {{ ±val }} |
| WTI 유가 | ${{ val }} | {{ ±val }} |

---

## ③ 미국 지수

| 지수 | 종가 | 등락률 |
|------|------|--------|
| S&P 500 | {{ val }} | {{ ±% }} |
| 나스닥 | {{ val }} | {{ ±% }} |
| 다우존스 | {{ val }} | {{ ±% }} |

---

## ④ 섹터별 동향

| 섹터 | 방향 | 대표 종목 | 주요 이슈 |
|------|------|-----------|-----------|
{{ watchlist.yaml sectors 기준으로 행 자동 생성 }}
※ 각 섹터 대표 종목은 해당 섹터 첫 번째 ticker 사용

---

## ⑤ 오늘의 핵심 키워드

① {{ 키워드1 }}: {{ 설명 1~2문장 }}
② {{ 키워드2 }}: {{ 설명 1~2문장 }}
③ {{ 키워드3 }}: {{ 설명 1~2문장 }}
④ {{ 키워드4 }}: {{ 설명 1~2문장 }}
⑤ {{ 키워드5 }}: {{ 설명 1~2문장 }}

---

## 📝 종합 요약

{{ 오늘 시장 전반 흐름 200자 내외. 수급 특이사항·글로벌 영향·내일 주목 이벤트 포함 }}
```

### 2-3. 시장 온도계 판단 기준

아래 기준을 **우선순위 순서대로** 적용하여 하나를 선택하라.

**🔴 리스크오프** — 다음 중 2개 이상 해당:
- 코스피 -1.0% 이상 하락
- 원/달러 환율 1,400원 초과
- 미 10년물 금리 당일 상승 + 4.5% 초과
- S&P 500 -1.0% 이상 하락
- 섹터 전체 중 75% 이상 🔴

**🟢 리스크온** — 다음 중 2개 이상 해당:
- 코스피 +1.0% 이상 상승
- 원/달러 환율 1,350원 미만
- S&P 500 +1.0% 이상 상승
- 외국인 순매수 우세 (뉴스 기반 판단)
- 섹터 전체 중 75% 이상 🟢

**🟡 중립** — 위 두 조건 미충족 시

### 2-4. 섹터 방향 판단 기준

- 🟢: 섹터 내 tickers 평균 등락률 +0.5% 초과
- 🔴: 섹터 내 tickers 평균 등락률 -0.5% 미만
- 🟡: 그 외 (-0.5% ~ +0.5% 범위)

---

## [Step 3: 파일 저장]

Write tool로 아래 경로에 리포트를 저장하라.
(clone된 repo root 기준 상대경로)

- 저장 경로: `reports/stock/stock_YYYY-MM-DD.md` (오늘 날짜 ISO 형식)
- 디렉토리가 없으면 생성 후 저장
- 이미 파일이 존재하면 덮어쓰기

---

## [Step 3.5: 종목 이력 — 동향 및 이슈 업데이트]

> **목적**: `reports/history/{ticker}.md` 파일의 `📰 동향 및 이슈` 섹션에 오늘 데이터를 추가한다.
> GitHub Actions(`update_history.py`)는 **가격 테이블**만 담당.
> 루틴은 **동향 이슈 텍스트**(맥락·키워드 포함)를 담당한다.

### 3.5-1. 처리 대상

`watchlist.yaml`의 **sectors** 항목만 처리한다 (indices / us_indices / macro 제외).
enabled: false 섹터는 건너뛴다.

### 3.5-2. 파일명 변환 규칙

| 티커 | 파일명 |
|------|--------|
| `NVDA` | `reports/history/NVDA.md` |
| `005930.KS` | `reports/history/005930.KS.md` |
| `BRK-B` | `reports/history/BRK-B.md` |

### 3.5-3. 각 종목 처리 순서

**① Read tool**로 해당 파일을 읽는다.
  - 파일이 없으면 건너뛴다 (Actions가 다음 빌드 시 생성).

**② 형식 확인**  
  - 파일에 `<!-- ISSUES_TOP -->` 마커가 없으면 → **건너뜀**  
    (레거시 형식. 다음 Actions 실행 후 마이그레이션되면 익일부터 정상 처리)

**③ 중복 확인**  
  - 파일에 이미 `### YYYY-MM-DD` (오늘 날짜) 섹션이 있으면 → **건너뜀** (중복 방지)

**④ Edit tool**로 `<!-- ISSUES_TOP -->` 를 아래 내용으로 교체한다.  
마커는 **남겨두고** 새 항목을 마커 바로 뒤에 삽입 → 최신 항목이 위로 쌓임.

```
<!-- ISSUES_TOP -->

### YYYY-MM-DD
- 종가: {price} | 등락: {change_pct}% {price_emoji}
- 섹터: {sector_name} {sector_emoji}
- 시장: {market_temp_emoji} {market_temp_label}
```

**실제 예시 (NVDA, 리스크온 날):**
```
<!-- ISSUES_TOP -->

### 2026-05-26
- 종가: 224.59 | 등락: +1.38% 🟢
- 섹터: 반도체 🟢
- 시장: 🟢 리스크온
```

**왜 이 방식인가:**  
마커가 항상 섹션 상단에 있으므로, 새 항목을 마커 직후에 삽입하면  
다음 날도 동일한 마커를 기준으로 그 위에 항목이 쌓여 최신순이 유지된다.

### 3.5-4. 값 결정 기준

| 항목 | 출처 |
|------|------|
| `price` | Step 1에서 수집한 해당 티커 종가 |
| `change_pct` | Step 1 수집값. N/A면 항목 전체 건너뜀 |
| `price_emoji` | +0.5% 초과=🟢 / -0.5% 미만=🔴 / 그 외=🟡 |
| `sector_emoji` | Step 2 리포트의 섹터 방향(🟢/🟡/🔴) 재사용 |
| `market_temp_emoji` / `label` | Step 2에서 판단한 시장 온도계 재사용 |

### 3.5-5. 오류 처리

- Read 실패 / 마커 없음 / 데이터 N/A → 해당 종목 건너뜀, **루틴 계속 진행**
- Edit 실패 → 경고 출력 후 다음 종목으로

---

## [Step 4: Notion 등록 — Notion MCP]

> **데이터베이스 ID 고정**: `362fae73-9c2b-80c2-ba08-c6f31ae8f922`
> (매번 notion-search로 탐색하지 않는다)

### 4-1. 중복 체크

`notion-search`로 오늘 날짜(`YYYY-MM-DD`)가 제목에 포함된 페이지를 먼저 조회한다.
- **이미 존재**: `notion-update-page`로 기존 페이지를 업데이트한다
- **없음**: `notion-create-pages`로 신규 생성한다

### 4-2. 페이지 속성 매핑

| Notion 속성명 | 타입 | 입력값 |
|--------------|------|--------|
| `이름` | title | `[YYYY-MM-DD] 주식 시황 브리핑` |
| `날짜` | date | 오늘 날짜 (`YYYY-MM-DD`) |
| `시장온도` | select | `리스크오프` / `중립` / `리스크온` (이모지 제외) |
| `코스피` | number | 코스피 종가 숫자 (N/A면 입력 생략) |
| `원달러` | number | 원/달러 환율 숫자 (N/A면 입력 생략) |
| `핵심키워드` | multi_select | 키워드 ①~⑤ 텍스트 5개 |
| `summary` | rich_text | 종합 요약 200자 내외 |
| `리포트링크` | url | GitHub Pages 발행 URL (빌드 전이면 생략) |

### 4-3. 실패 처리

Notion API 오류 발생 시 오류 내용을 콘솔에 출력하고 **루틴은 계속 진행**한다.

---

## [Step 5: Git 커밋 + 푸시 — Bash tool]

```bash
TODAY=$(date +%Y-%m-%d)
git add reports/stock/ reports/history/
git commit -m "📊 Stock briefing ${TODAY}"
git push
```

push 결과(성공/실패)를 콘솔에 출력하라.
push 실패 시에도 루틴은 정상 종료한다.

> **GitHub Actions 연계 (역할 분담)**
> - **루틴**: `reports/stock/` (새 리포트) + `reports/history/` (동향 이슈 섹션)
> - **Actions(`update_history.py`)**: `reports/history/` 가격 테이블 + 레거시 마이그레이션
>
> git push 성공 후 `stock_build.yml` 워크플로우가 자동 트리거되어
> 웹페이지 빌드·이메일 발송·가격 테이블 업데이트를 처리한다. 루틴에서 별도 실행 불필요.

---

## [실행 흐름 요약]

```
[스케줄러 자동 실행]
        │
        ▼
[Step 1] watchlist.yaml 로드 → PlayMCP 수집 (전체 티커 + 뉴스 4쿼리)
         └─ 개별 실패 → N/A 처리 후 계속
        │
        ▼
[Step 2] 리포트 작성
         ├─ 템플릿 로드 성공 → 템플릿 구조 사용
         └─ 템플릿 로드 실패 → 내장 구조 fallback
         └─ 섹터 방향·시장온도 계산 (Step 3.5에서 재사용)
        │
        ▼
[Step 3] Write tool → reports/stock/stock_YYYY-MM-DD.md 저장
        │
        ▼
[Step 3.5] 종목 이력 동향·이슈 업데이트
         ├─ sectors 종목 순회 (enabled만)
         ├─ 각 파일에 ISSUES_APPEND_END 마커 확인
         │    ├─ 마커 없음(레거시) → 건너뜀 (Actions가 마이그레이션)
         │    └─ 마커 있음 → Edit tool로 오늘 이슈 항목 prepend
         └─ 개별 실패 → 건너뜀, 루틴 계속
        │
        ▼
[Step 4] Notion MCP → 중복 체크 후 create / update
         └─ 실패해도 루틴 계속 (비중단)
        │
        ▼
[Step 5] git add reports/stock/ reports/history/ → commit → push
         └─ GitHub Actions stock_build.yml 자동 트리거
              ├─ update_history.py: 가격 테이블 업데이트 + 레거시 마이그레이션
              ├─ 웹페이지 빌드
              └─ 이메일 발송
```

---

## [사전 설정 체크리스트]

- [ ] 루틴에 repo 연결 확인
- [ ] 루틴 설정에서 **"Allow unrestricted branch pushes"** 활성화
       (미설정 시 `claude/`-접두사 브랜치에만 push됨 → Actions 미트리거)
- [ ] `.claude/config/watchlist.yaml` repo에 커밋 확인
- [ ] Notion DB(`362fae73-...`)에 속성 8개 추가 완료 확인
- [ ] `stock_build.yml` 트리거 경로에 `reports/stock/**` 포함 확인
- [ ] `templates/stock_report.md` 없어도 내장 구조로 동작 (선택사항)
- [ ] `templates/stock_history.md` 없어도 인라인 폴백으로 동작 (선택사항)
- [ ] 기존 레거시 history 파일은 최초 Actions 실행 시 자동 마이그레이션됨
      (루틴은 마이그레이션 이후부터 동향 이슈 섹션을 기록 시작)